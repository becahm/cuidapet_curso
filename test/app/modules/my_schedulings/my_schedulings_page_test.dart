import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:cuidapet_curso/app/modules/my_schedulings/my_schedulings_page.dart';

void main() {
  testWidgets('MySchedulingsPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(MySchedulingsPage(title: 'MySchedulings')));
    //  final titleFinder = find.text('MySchedulings');
    //  expect(titleFinder, findsOneWidget);
  });
}
