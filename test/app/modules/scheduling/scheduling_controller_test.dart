import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:cuidapet_curso/app/modules/scheduling/scheduling_controller.dart';
import 'package:cuidapet_curso/app/modules/scheduling/scheduling_module.dart';

void main() {
  initModule(SchedulingModule());
  // SchedulingController scheduling;
  //
  setUp(() {
    //     scheduling = SchedulingModule.to.get<SchedulingController>();
  });

  group('SchedulingController Test', () {
    //   test("First Test", () {
    //     expect(scheduling, isInstanceOf<SchedulingController>());
    //   });

    //   test("Set Value", () {
    //     expect(scheduling.value, equals(0));
    //     scheduling.increment();
    //     expect(scheduling.value, equals(1));
    //   });
  });
}
