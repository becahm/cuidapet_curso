import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:cuidapet_curso/app/modules/scheduling/scheduling_page.dart';

void main() {
  testWidgets('SchedulingPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(SchedulingPage(title: 'Scheduling')));
    //  final titleFinder = find.text('Scheduling');
    //  expect(titleFinder, findsOneWidget);
  });
}
