import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:cuidapet_curso/app/modules/home/addresses/addresses_page.dart';

void main() {
  testWidgets('AddressesPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(AddressesPage(title: 'Addresses')));
    //  final titleFinder = find.text('Addresses');
    //  expect(titleFinder, findsOneWidget);
  });
}
