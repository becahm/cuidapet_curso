import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:cuidapet_curso/app/modules/provider/provider_page.dart';

void main() {
  testWidgets('ProviderPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ProviderPage(title: 'Provider')));
    //  final titleFinder = find.text('Provider');
    //  expect(titleFinder, findsOneWidget);
  });
}
