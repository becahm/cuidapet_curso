import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:cuidapet_curso/app/modules/provider/provider_controller.dart';
import 'package:cuidapet_curso/app/modules/provider/provider_module.dart';

void main() {
  initModule(ProviderModule());
  // ProviderController provider;
  //
  setUp(() {
    //     provider = ProviderModule.to.get<ProviderController>();
  });

  group('ProviderController Test', () {
    //   test("First Test", () {
    //     expect(provider, isInstanceOf<ProviderController>());
    //   });

    //   test("Set Value", () {
    //     expect(provider.value, equals(0));
    //     provider.increment();
    //     expect(provider.value, equals(1));
    //   });
  });
}
